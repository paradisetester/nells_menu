export const CREATE_MENU_COMMENT = 'New comment created by ${CREATED_BY} for ${MENU_ITEM}';
export const DELETE_MENU_COMMENT = 'Comment deleted by ${DELETED_BY} for ${MENU_ITEM}';
export const UPDATE_MENU_COMMENT = 'Comment updated by ${UPDATED_BY} for ${MENU_ITEM}';