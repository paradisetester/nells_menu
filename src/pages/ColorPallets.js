import { PublicLayout } from "../components/layouts";
import { ColorPallet } from "../components/pallet";

export default function ColorPallets() {
    return (
        <PublicLayout>
            <ColorPallet />
        </PublicLayout>
    );
}